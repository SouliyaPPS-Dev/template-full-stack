---
name: full-stack-flow
description: Complete full-stack business flow orchestrator. Creates connected Frontend, Backend, Mobile App, and Database systems with unified data flow. Use when building complete full-stack applications with business logic spanning all layers. Triggers on: full-stack, business flow, complete app, end-to-end, connected systems.
---

# Full Stack Flow - Business Flow Orchestrator

Master skill for creating complete full-stack systems with unified business flow.

## Purpose

Act as your **Personal CTO** - orchestrating complete full-stack development where:
- Frontend ↔ Backend ↔ Mobile ↔ Database are all connected
- Business logic flows seamlessly across all layers
- Data structures are unified and consistent
- API contracts are shared between all clients

## When to Use

- Creating a new full-stack application from scratch
- Adding new features that span multiple layers
- Refactoring to connect disconnected systems
- Building MVP with complete business flow

## Actual Project Structure (Verified)

```
/
├── apps/
│   └── web/                          # Single React app (admin + user)
│       ├── src/
│       │   ├── routes/
│       │   │   ├── __root.tsx        # Root route (QueryClientProvider, HTML shell)
│       │   │   ├── _admin.tsx        # Admin layout route (auth guard + AdminLayout)
│       │   │   ├── _admin/admin/     # Admin pages (flat files)
│       │   │   │   ├── index.tsx     # /admin (Dashboard)
│       │   │   │   ├── login.tsx     # /admin/login
│       │   │   │   ├── products.tsx  # /admin/products
│       │   │   │   ├── orders.tsx    # /admin/orders
│       │   │   │   ├── customers.tsx # /admin/customers
│       │   │   │   └── settings.tsx  # /admin/settings
│       │   │   ├── _user.tsx         # User layout route (navbar + mobile menu)
│       │   │   └── _user/            # User pages
│       │   │       ├── index.tsx     # / (home)
│       │   │       ├── login.tsx     # /login
│       │   │       ├── products.tsx  # /products
│       │   │       ├── cart.tsx      # /cart
│       │   │       └── profile.tsx   # /profile
│       │   ├── components/
│       │   │   ├── admin-layout.tsx  # Admin sidebar + Outlet
│       │   │   └── ui/               # shadcn/ui components
│       │   │       ├── button.tsx
│       │   │       ├── card.tsx
│       │   │       ├── input.tsx
│       │   │       └── label.tsx
│       │   ├── hooks/
│       │   │   ├── use-auth.ts       # useAuth() - reads user from localStorage
│       │   │   └── use-auth-monitor.ts # Token expiry monitor
│       │   ├── services/
│       │   │   └── api.ts            # All API functions + types
│       │   ├── lib/
│       │   │   └── utils.ts          # cn() helper (clsx + tailwind-merge)
│       │   ├── router.tsx            # createRouter with routeTree
│       │   ├── routeTree.gen.ts      # Auto-generated by TanStack Router
│       │   └── index.css             # Tailwind v4 + @theme variables
│       ├── package.json
│       ├── vite.config.ts
│       └── tsconfig.json
│
├── services/
│   ├── api-gateway/                  # Go (Chi router) - port 8080
│   │   ├── cmd/main.go              # Entry point, route registration
│   │   ├── internal/
│   │   │   ├── config/              # env-based config
│   │   │   ├── database/            # PostgreSQL connection (lib/pq)
│   │   │   ├── handlers/            # HTTP handlers (auth, products, users, etc.)
│   │   │   ├── middleware/          # Auth, AdminOnly, RateLimit, SecurityHeaders
│   │   │   └── models/models.go    # All struct definitions
│   │   └── go.mod                   # go-chi/chi, lib/pq, golang-jwt
│   │
│   ├── auth-service/                 # Python (FastAPI) - optional
│   │   ├── app/
│   │   │   ├── api/                 # auth.py, users.py routers
│   │   │   ├── core/                # config.py (pydantic-settings)
│   │   │   ├── models/              # SQLAlchemy/Pydantic models
│   │   │   └── services/            # Business logic
│   │   └── pyproject.toml           # Poetry deps
│   │
│   └── data-service/                 # Rust (Axum) - optional
│       ├── src/
│       │   ├── main.rs
│       │   ├── handlers/
│       │   ├── models/
│       │   ├── routes/
│       │   └── services/
│       └── Cargo.toml
│
├── database/
│   ├── schema.sql                    # PostgreSQL schema (full-stack)
│   ├── migrations/
│   └── seeds/
│
├── packages/shared/                  # Shared types (optional)
├── infra/docker/                     # Docker configs
└── .opencode/skills/                 # Agent skills
```

## Execution Flow

### Phase 1: Business Analysis
```
1. Understand the business requirement
2. Identify all entities and relationships
3. Map data flow: User → Frontend → API → Backend → Database → Response
4. Define API contracts (Go handlers)
```

### Phase 2: Database Design
```
1. Design PostgreSQL schema with UUIDs
2. Create database/schema.sql
3. Define relationships (1:1, 1:N, N:M)
4. Add indexes for performance
5. Create seed data
```

### Phase 3: Backend API (Go Chi Router)
```
1. Define models in internal/models/models.go
2. Create handlers in internal/handlers/
3. Register routes in cmd/main.go under /api/v1
4. Add middleware (Auth, AdminOnly, RateLimit)
5. Use database.DB for PostgreSQL queries
```

### Phase 4: Frontend (Web Admin + User)
```
1. Add TypeScript interface in services/api.ts
2. Add API function in services/api.ts
3. Create route file in routes/_admin/admin/ or routes/_user/
4. Use createFileRoute() with loader + component
5. Use useQuery() for client-side data fetching
6. Build UI with shadcn/ui components (Card, Button, Input, Label)
```

### Phase 5: Mobile App (Optional)
```
1. Share types with frontend
2. Create API service layer
3. Build screens matching web functionality
```

### Phase 6: Integration & Testing
```
1. Connect all layers end-to-end
2. Verify API endpoints with curl
3. Test admin/user flows
```

## Key Patterns (Verified from Codebase)

### Frontend: TanStack Router File-Based Routes

```tsx
// Layout route with auth guard (_admin.tsx)
import { createFileRoute, redirect, Outlet } from "@tanstack/react-router";
import { AdminLayout } from "@/components/admin-layout";
import { adminIsAuthenticated, getUser } from "@/services/api";

export const Route = createFileRoute("/_admin")({
  beforeLoad: ({ location }) => {
    if (!isClient()) return;
    if (location.pathname === "/admin/login") return;
    if (!adminIsAuthenticated()) {
      throw redirect({ to: "/admin/login" });
    }
    const user = getUser("admin");
    if (user && user.role !== "admin" && user.role !== "superadmin") {
      throw redirect({ to: "/admin/login" });
    }
  },
  component: AdminShell,
});
```

### Frontend: Page Route with Loader + Query

```tsx
// Admin page (products.tsx)
import { createFileRoute } from "@tanstack/react-router";
import { useQuery } from "@tanstack/react-query";
import { Card, CardContent } from "@/components/ui/card";
import { api, getApiBase } from "@/services/api";

interface Product {
  id: string;
  name: string;
  slug: string;
  selling_price: number;
  stock: number;
  is_active: boolean;
}

async function loader() {
  try {
    const base = getApiBase();
    const res = await fetch(`${base}/products`);
    if (!res.ok) return { products: [] as Product[] };
    return { products: await res.json() };
  } catch {
    return { products: [] as Product[] };
  }
}

export const Route = createFileRoute("/_admin/admin/products")({
  component: AdminProducts,
  loader,
});

function AdminProducts() {
  const { products: serverProducts } = Route.useLoaderData();
  const { data: clientProducts, isLoading } = useQuery({
    queryKey: ["admin-products"],
    queryFn: () => api<Product[]>("/products", undefined, "admin"),
  });
  const products = clientProducts || serverProducts;
  // ... render table
}
```

### Frontend: API Service (services/api.ts)

```tsx
const API_BASE = import.meta.env.VITE_API_URL || "http://localhost:8080/api/v1";

export type UserType = "user" | "admin";

export interface User {
  id: string;
  email: string;
  full_name: string;
  phone: string;
  role: string;
  is_active: boolean;
  created_at: string;
}

export async function api<T>(path: string, options?: RequestInit, userType: UserType = "user"): Promise<T> {
  const token = isClient() ? localStorage.getItem(tokenKey(userType)) : null;
  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...((options?.headers as Record<string, string>) || {}),
  };
  if (token) headers["Authorization"] = `Bearer ${token}`;

  const res = await fetch(`${API_BASE}${path}`, { ...options, headers });
  if (res.status === 401) { /* clear auth */ throw new Error("Unauthorized"); }
  if (!res.ok) { const err = await res.json().catch(() => ({})); throw new Error(err.error || "API error"); }
  return res.json();
}
```

### Backend: Go Handler Pattern

```go
// internal/handlers/users.go
package handlers

import (
    "net/http"
    "github.com/myorg/api-gateway/internal/database"
    "github.com/myorg/api-gateway/internal/models"
)

func ListUsers(w http.ResponseWriter, r *http.Request) {
    rows, err := database.DB.Query("SELECT id, email, full_name, COALESCE(phone,''), role, email_verified, is_active, created_at FROM users ORDER BY created_at DESC")
    if err != nil {
        writeError(w, http.StatusInternalServerError, "query failed")
        return
    }
    defer rows.Close()

    users := []models.User{}
    for rows.Next() {
        var u models.User
        rows.Scan(&u.ID, &u.Email, &u.FullName, &u.Phone, &u.Role, &u.EmailVerified, &u.IsActive, &u.CreatedAt)
        users = append(users, u)
    }
    writeJSON(w, http.StatusOK, users)
}
```

### Backend: Route Registration (cmd/main.go)

```go
r.Route("/api/v1", func(r chi.Router) {
    // Public
    r.Get("/products", handlers.ListProducts)

    // Protected
    r.Group(func(r chi.Router) {
        r.Use(middleware.Auth(cfg))
        r.Get("/auth/me", handlers.Me)
        r.With(middleware.AdminOnly).Get("/users", handlers.ListUsers)
        r.With(middleware.AdminOnly).Post("/products", handlers.CreateProduct)
        r.With(middleware.AdminOnly).Put("/products/{id}", handlers.UpdateProduct)
        r.With(middleware.AdminOnly).Delete("/products/{id}", handlers.DeleteProduct)
    })
})
```

### Backend: Models (internal/models/models.go)

```go
type User struct {
    ID            string `json:"id"`
    Email         string `json:"email"`
    FullName      string `json:"full_name"`
    Phone         string `json:"phone"`
    Role          string `json:"role"`
    EmailVerified bool   `json:"email_verified"`
    IsActive      bool   `json:"is_active"`
    CreatedAt     string `json:"created_at"`
}
```

### Database: PostgreSQL Schema Pattern

```sql
CREATE TABLE users (
    id              UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    email           VARCHAR(255) UNIQUE NOT NULL,
    phone           VARCHAR(20) UNIQUE,
    password_hash   VARCHAR(255) NOT NULL,
    full_name       VARCHAR(255) NOT NULL,
    avatar_url      TEXT,
    role            VARCHAR(20) NOT NULL DEFAULT 'user'
                    CHECK (role IN ('user', 'staff', 'admin', 'superadmin')),
    email_verified  BOOLEAN DEFAULT FALSE,
    is_active       BOOLEAN DEFAULT TRUE,
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW(),
    deleted_at      TIMESTAMPTZ
);
```

### Frontend: Admin Layout Sidebar

```tsx
// components/admin-layout.tsx
const navItems = [
  { label: "Dashboard", path: "/admin", icon: LayoutDashboard },
  { label: "Products", path: "/admin/products", icon: Package },
  { label: "Orders", path: "/admin/orders", icon: ShoppingCart },
  { label: "Customers", path: "/admin/customers", icon: Users },
  { label: "Settings", path: "/admin/settings", icon: Settings },
];

export function AdminLayout() {
  // Sidebar with navItems, user info, logout button
  // Main content via <Outlet />
}
```

## Tech Stack (Actual)

| Layer | Technology | Details |
|-------|-----------|---------|
| Frontend | React 19 + TypeScript | Single app (`apps/web`) for admin + user |
| Router | TanStack Router | File-based routes, `_admin.tsx` layout pattern |
| Data Fetching | TanStack Query | `useQuery()` + `useMutation()` |
| UI | shadcn/ui | Button, Card, Input, Label (manual copies) |
| Styling | Tailwind CSS v4 | `@theme` variables in `index.css` |
| Bundler | Vite 8 | With tanstackStart plugin |
| API Gateway | Go + Chi | `services/api-gateway/`, port 8080 |
| Auth Service | Python + FastAPI | `services/auth-service/` (optional) |
| Data Service | Rust + Axum | `services/data-service/` (optional) |
| Database | PostgreSQL | UUID PKs, JSONB, TIMESTAMPTZ |
| Package Mgr | Bun | `bun install`, `bun run dev` |
| PWA | vite-plugin-pwa | Service worker + manifest |

## API Endpoints (Go Gateway)

```
GET    /health                          → Health check
POST   /api/v1/auth/register            → Register (rate limited)
POST   /api/v1/auth/login               → Login (rate limited)
GET    /api/v1/auth/me                  → Current user (auth required)
PUT    /api/v1/auth/me                  → Update profile (auth required)
GET    /api/v1/products                 → List products (public)
POST   /api/v1/products                 → Create product (admin)
GET    /api/v1/products/{id}            → Get product (auth)
PUT    /api/v1/products/{id}            → Update product (admin)
DELETE /api/v1/products/{id}            → Delete product (admin)
GET    /api/v1/categories               → List categories (public)
POST   /api/v1/categories               → Create category (admin)
GET    /api/v1/categories/{id}          → Get category (auth)
PUT    /api/v1/categories/{id}          → Update category (admin)
DELETE /api/v1/categories/{id}          → Delete category (admin)
GET    /api/v1/orders                   → List orders (auth)
POST   /api/v1/orders                   → Create order (auth)
GET    /api/v1/orders/{id}              → Get order (auth)
PUT    /api/v1/orders/{id}              → Update order (admin)
GET    /api/v1/users                    → List users (admin)
GET    /api/v1/quotations               → List quotations (auth)
POST   /api/v1/quotations               → Create quotation (auth)
GET    /api/v1/dashboard/stats          → Dashboard stats (admin)
GET    /api/v1/settings                 → System settings (public)
```

## Commands

### Development
```bash
bun run dev:api          # Start Go API gateway (port 8080)
bun run dev:web          # Start Vite dev server (port 3000)
bun run dev:admin        # Start admin dev server
bun run dev:flutter      # Start Flutter app
bun run dev:rn           # Start React Native app
bun run stop             # Stop all services
```

### Database
```bash
psql -U app_user -d app_main -f database/schema.sql
```

### Build
```bash
bun run build            # Build web + admin
bun run test             # Run all tests
```

## Adding a New Feature (Step by Step)

### 1. Database
Add table to `database/schema.sql` with UUID PK, timestamps, indexes.

### 2. Backend (Go)
- Add struct to `services/api-gateway/internal/models/models.go`
- Create handler in `services/api-gateway/internal/handlers/`
- Register routes in `services/api-gateway/cmd/main.go`

### 3. Frontend API
- Add interface + API function to `apps/web/src/services/api.ts`

### 4. Frontend Page
- Create route file: `apps/web/src/routes/_admin/admin/{feature}.tsx`
- Use `createFileRoute()` with `loader` + `component`
- Use `useQuery()` for data, shadcn/ui for UI

### 5. Navigation
- Add nav item to `apps/web/src/components/admin-layout.tsx` navItems array

## Integration with Other Skills

- **shadcn**: Use for adding new UI components to `src/components/ui/`
- **tanstack-router**: Use for route patterns and navigation
- **mysql-database**: For MySQL-specific patterns (PHP legacy projects)
- **php-backend**: For PHP MVC patterns (XAMPP projects)
- **function-memory**: Store API calls and responses for caching
